import React, { Component } from 'react'; //allows use of react library
import './Footer.css'; //imports css code from Footer.css

class Footer extends Component {//creates a component by extending Component from react library
  render() {//method for rendering component to the virtual dom
    return (
        <footer className="Footer">
        </footer>

    );
  }
}

export default Footer; //export Footer to be used elsewhere in the code
